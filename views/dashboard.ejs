<!DOCTYPE html>
<html lang="en">

<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Miary</title>

        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
                integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
                crossorigin="anonymous">
        <link rel="stylesheet" href="dashboard.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>

</head>

<body>
        <div class="container">
                <div class="row">
                        <div class="col-12">
                                <h1 class="mb-2 mt-5">Welcome, <%= user.fname %> </h1>
                                <p class="mb-3 form"><%- postStatus %> </p>
                                <hr>
                        </div>
                </div>
                <div class="row submission-form">
                        <div class="col-12 col-md-8">
                                <form action="/<%= user._id %>" method="POST">
                                        <h1 class="h3 font-weight-normal"><%= day %></h1>
                                        <div class="form-group">
                                                <input type="text" name="title" class="form-control "
                                                        placeholder="Title" required autofocus autocomplete="off">
                                        </div>
                                        <div class="form-group">
                                                <textarea name="content" class="form-control "
                                                        placeholder="How was your day?" required rows="6" autofocus
                                                        autocomplete="off">How was your day?</textarea>
                                        </div>
                                        <button class="btn btn-outline-light " type="submit">Submit</button>
                                </form>
                        </div>
                </div>

                <div class="container">
                        <h3><%= message %></h3>
                        <% user.posts.slice().reverse().forEach(function(post){ %>
                        <div class="row">
                                <div class="col-12">
                                        <div class="card-body mt-3 mb-3">
                                                <h5 class="card-title"><%= post.title %> </h5>
                                                <h6 class="card-subtitle mb-2 text-muted"><%= post.date %></h6>
                                                <p class="card-text"><%= post.content %> </p>
                                        </div>
                                </div>
                        </div>
                        <% })   %>
                </div>
        </div>

        </div>
        <script>
                $(document).ready(function () {

                        $(".form").click(function () {
                                //need the following to toggle
                                $(".submission-form").css("display", "block");
                        });
                });
        </script>
        <%- include('footer'); %>